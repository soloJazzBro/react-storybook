import image from '../src/assets/images/ux.svg'
import { create } from '@storybook/theming';

export default create({
  base: 'dark',
  brandTitle: 'UX React-Bootstrap Component Library',
  brandUrl: 'https://example.com',
  brandImage: image,
});